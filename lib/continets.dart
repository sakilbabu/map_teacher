import 'package:flutter/material.dart';

class Continets extends StatelessWidget {
  const Continets({Key? key, required String classModel}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("lool"),
    );
  }
}
